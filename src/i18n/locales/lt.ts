import type { TranslationDictionary } from '../types';
import { en } from './en';

/** Lithuanian (lietuvių) locale scaffold. */
export const lt: TranslationDictionary = {
  ...en,
  languages: {
    en: 'Anglų',
    hu: 'Vengrų',
    de: 'Vokiečių'
  }
};
