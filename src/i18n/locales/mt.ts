import type { TranslationDictionary } from '../types';
import { en } from './en';

/** Maltese (Malti) locale scaffold. */
export const mt: TranslationDictionary = {
  ...en,
  languages: {
    en: 'Ingliż',
    hu: 'Ungeriż',
    de: 'Ġermaniż'
  }
};
